{{- if .Values.leaderElection.enabled }}
apiVersion: coordination.k8s.io/v1
kind: Lease
metadata:
  name: {{ include "k8s-aws-oidc-chart.leaseName" . }}
  labels:
    {{- include "k8s-aws-oidc-chart.labels" . | nindent 4 }}
spec: {}
{{- end }}
